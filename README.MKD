# 🎨 ICNS Converter

Herramienta Python para convertir imágenes a formato ICNS (iconos de macOS) con soporte para transparencia, procesamiento por lotes y conversión paralela.

## ✨ Características

- 🖼️ **Conversión de alta calidad** con algoritmo LANCZOS
- 🌈 **Preservación de transparencia** (canal alpha)
- ⚡ **Procesamiento paralelo** para conversiones por lotes
- 📁 **Búsqueda recursiva** en subcarpetas
- 🎯 **Múltiples formatos** soportados: PNG, JPG, JPEG, BMP, TIFF, GIF, WebP
- 🔧 **Interfaz CLI completa** con argumentos personalizables
- ✅ **Validación automática** de requisitos y tamaños

## 📋 Requisitos

- **macOS** (requiere la utilidad `iconutil` del sistema)
- **Python 3.7+**
- **Pillow** (PIL)

### Instalación de dependencias

```bash
pip install Pillow
```

## 🚀 Uso

### Conversión simple

```bash
# Convertir una imagen (genera pokeball.icns en la misma carpeta)
python3 convert_to_icns.py pokeball.png

# Especificar archivo de salida
python3 convert_to_icns.py pokeball.png -o ~/Desktop/mi_icono.icns
```

### Conversión por lotes

```bash
# Convertir todas las imágenes de una carpeta
python3 convert_to_icns.py ./imagenes/ -o ./iconos/

# Búsqueda recursiva en subcarpetas con 8 workers
python3 convert_to_icns.py ./imagenes/ -o ./iconos/ -r --workers 8
```

### Opciones avanzadas

```bash
# Sin transparencia (fondo blanco)
python3 convert_to_icns.py logo.png --no-alpha

# Ajustar calidad de compresión
python3 convert_to_icns.py imagen.png -q 100

# Ver todas las opciones
python3 convert_to_icns.py --help
```

## 📖 Argumentos

| Argumento | Descripción |
|-----------|-------------|
| `input` | Archivo de imagen o directorio (requerido) |
| `-o, --output` | Archivo o directorio de salida |
| `-r, --recursive` | Buscar imágenes en subcarpetas |
| `--no-alpha` | No preservar transparencia (usar fondo blanco) |
| `-w, --workers` | Número de conversiones paralelas (default: 4) |
| `-q, --quality` | Calidad de compresión 1-100 (default: 95) |
| `-h, --help` | Mostrar ayuda |

## 💡 Ejemplos

### Crear ícono de aplicación

```bash
# Desde un PNG con transparencia
python3 convert_to_icns.py app_logo.png -o MiApp.icns
```

### Convertir carpeta completa

```bash
# Convertir todas las imágenes de 'assets' a 'icons'
python3 convert_to_icns.py ./assets/ -o ./icons/ -r --workers 8
```

### Uso como módulo Python

```python
from convert_to_icns import ICNSConverter

# Crear conversor
converter = ICNSConverter(preserve_alpha=True, quality=95)

# Convertir una imagen
converter.convert('logo.png', 'output.icns')

# Conversión por lotes
converter.batch_convert(
    input_folder='./imagenes/',
    output_folder='./iconos/',
    recursive=True,
    max_workers=4
)
```

## 🎯 Formatos soportados

- PNG (recomendado para transparencia)
- JPEG / JPG
- BMP
- TIFF / TIF
- GIF
- WebP

## 📐 Tamaños generados

El script genera automáticamente todos los tamaños estándar de ICNS:

- 16x16, 32x32, 64x64, 128x128, 256x256, 512x512, 1024x1024
- Versiones @2x (Retina) para 16, 32, 128, 256, 512

## ⚠️ Notas importantes

- La imagen de entrada debe tener **al menos 16x16 píxeles**
- Para mejores resultados, usa imágenes cuadradas de **1024x1024 o más**
- Las imágenes con transparencia deben estar en formato PNG
- El script solo funciona en **macOS** (requiere `iconutil`)

## 🐛 Solución de problemas

### "iconutil no está disponible"
- Este script solo funciona en macOS
- Verifica que `iconutil` esté en tu PATH

### "La imagen debe tener al menos 16x16 píxeles"
- Tu imagen es demasiado pequeña
- Usa una imagen de mayor resolución

### Error de permisos en carpeta de salida
- Usa rutas relativas (`./iconos/`) en lugar de absolutas (`/iconos/`)
- Asegúrate de tener permisos de escritura en el directorio

## 📄 Licencia

MIT License - Ver archivo [LICENSE](LICENSE) para más detalles.

## 🤝 Contribuciones

Las contribuciones son bienvenidas. Por favor:

1. Fork el proyecto
2. Crea una rama para tu feature (`git checkout -b feature/AmazingFeature`)
3. Commit tus cambios (`git commit -m 'Add some AmazingFeature'`)
4. Push a la rama (`git push origin feature/AmazingFeature`)
5. Abre un Pull Request

## 👤 Autor

Desarrollado con ❤️ para la comunidad macOS

## 🙏 Agradecimientos

- Basado en las especificaciones de formato ICNS de Apple
- Usa la biblioteca Pillow para procesamiento de imágenes
- Utiliza la herramienta nativa `iconutil` de macOS

---

**¿Encontraste útil esta herramienta? ⭐ Dale una estrella al repositorio!**